import React from 'react';

/*
export const Test = () =>{
   return(
    <p>Test</p>
   )
   
}
// **onChange**
export const Person = (props) => {
    return (
        <>
            <p onClick={props.click}>Im {props.name}, I am {props.age} years old</p>
            <p>{props.children}</p>
            <input type='text' onChange={props.changed} value={props.name}/>
        </>
    )
}
*/
